@model CMS.Models.Post

@{
    ViewBag.Title = "Create";
}

<h2>Create New Category</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Post</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.CatID, "Category")
        </div>
        <div class="editor-field">
            @Html.DropDownList("CatID", String.Empty)
            @Html.ValidationMessageFor(model => model.CatID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.PostPrio)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.PostPrio)
            @Html.ValidationMessageFor(model => model.PostPrio)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ContentID, "Content")
        </div>
        <div class="editor-field">
            @Html.DropDownList("ContentID", String.Empty)
            @Html.ValidationMessageFor(model => model.ContentID)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
